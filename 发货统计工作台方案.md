# 发货统计与数据分析工作台 · 方案设计

> 文档状态：方案阶段（待评审）
> 目标形态：跨平台桌面应用（Windows / macOS）
> 核心目标：每日发货数据的采集、统计、可视化与多维对比分析

---

## 1. 产品定位

这是一个面向**电商 / 仓储 / 物流运营人员**的本地桌面工作台，用来解决"每天发货数据散落在多个平台导出表、人工汇总慢、对比分析难"的痛点。

核心价值可以用三句话概括：

- **收得进来**：支持从 Excel / CSV 批量导入，或对接主流电商平台与 ERP 导出的发货明细。
- **看得清楚**：自动生成每日、每周、每月的发货看板，核心指标一目了然。
- **比得明白**：支持环比、同比、目标达成率、渠道/品类/区域等多维度对比，发现异常与趋势。

数据默认**全部本地存储**，不上传云端，适合对数据隐私敏感的中小团队。

---

## 2. 核心功能模块

| 模块 | 功能描述 | 优先级 |
|------|----------|--------|
| 数据接入 | Excel/CSV 导入、字段映射、自动去重、历史数据追加 | P0 |
| 发货看板 | 日/周/月总览，核心 KPI 卡片 + 趋势图 | P0 |
| 多维分析 | 按渠道、品类、区域、物流商切片下钻 | P0 |
| 对比分析 | 环比 / 同比 / 目标达成率 / 自定义区间对比 | P0 |
| 异常预警 | 发货量骤降、准时率跌破阈值、异常订单标记 | P1 |
| 报表导出 | 看板导出为图片 / PDF，明细导出 Excel | P1 |
| 数据源对接 | 对接平台 API / ERP（可选，需凭据） | P2 |
| 多账套 | 支持多店铺 / 多仓库数据隔离管理 | P2 |

---

## 3. 关键指标（KPI）设计

工作台默认追踪以下核心指标，可在设置中增减：

| 指标 | 含义 | 典型用途 |
|------|------|----------|
| 发货单量 | 当日成功发货订单数 | 衡量业务规模 |
| 发货件数 | 发出的实物包裹 / 商品总件数 | 仓储产能规划 |
| 发货金额 | 发货订单总金额 | 营收跟踪 |
| 客单价 | 金额 / 单量 | 结构健康度 |
| 准时发货率 | 在承诺时效内发货占比 | 履约质量 |
| 异常订单数 | 缺货 / 退回 / 拦截等 | 运营风险防控 |

---

## 4. 对比分析能力（本工作台的重点）

这是区别于普通报表工具的核心。计划提供四类对比：

1. **环比对比**：今日 vs 昨日、本月 vs 上月，自动计算增幅（%）。
2. **同比对比**：今年某日 vs 去年同日，识别季节性规律。
3. **目标达成率**：预设日/月目标，直观显示完成进度（进度条 + 缺口）。
4. **自定义区间对比**：任意选两段时期，比较总量与结构差异（如"双11 周" vs "618 周"）。

所有对比结果都可一键导出，方便周报 / 月报直接引用。

---

## 5. 技术架构

### 5.1 总体架构

```
┌─────────────────────────────────────────┐
│           桌面应用（前端 UI）            │
│  React + TypeScript + Ant Design + ECharts │
└───────────────┬─────────────────────────┘
                │ 本地 IPC 调用
┌───────────────┴─────────────────────────┐
│          桌面壳层（Electron）           │
│  窗口管理 / 文件导入 / 系统托盘 / 自动更新 │
└───────────────┬─────────────────────────┘
                │ 本地数据库读写
┌───────────────┴─────────────────────────┐
│      本地数据库 SQLite（数据不出本机）   │
└─────────────────────────────────────────┘
```

### 5.2 技术选型建议

| 层级 | 推荐方案 | 理由 |
|------|----------|------|
| 桌面壳 | **Electron** | Web 技术栈、生态成熟、跨平台打包简单，适合数据可视化类应用 |
| 前端框架 | React + TypeScript | 组件生态丰富，便于做复杂看板 |
| UI 组件 | Ant Design | 企业级开箱即用，中文文档友好 |
| 图表 | ECharts | 国内发货/物流场景常用，图表类型全、交互强 |
| 本地存储 | SQLite（better-sqlite3） | 单机零部署、支持 SQL 分析、文件即数据库便于备份 |
| 表格处理 | SheetJS (xlsx) | 解析各平台导出的 Excel/CSV |

> 备选：若后续更看重安装包体积与启动速度，可平滑切换到 **Tauri（Rust 内核，更轻量）**，前端代码基本复用。

---

## 6. 数据模型（初稿）

核心表 `shipments`（发货明细，一行一笔发货记录）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | 自增 | 主键 |
| order_no | 文本 | 订单号（去重依据） |
| ship_date | 日期 | 发货日期 |
| channel | 文本 | 销售渠道（淘宝/京东/抖音/线下…） |
| category | 文本 | 商品品类 |
| sku | 文本 | 商品编码 |
| province | 文本 | 收货省份 |
| quantity | 整数 | 件数 |
| amount | 实数 | 金额 |
| carrier | 文本 | 物流商 |
| is_on_time | 布尔 | 是否准时发货 |
| is_exception | 布尔 | 是否异常订单 |

辅助表：`targets`（目标值）、`channels`（渠道字典）等。

---

## 7. 界面布局草案

```
┌─────────┬──────────────────────────────────────┐
│         │  顶部：日期选择器 / 店铺切换 / 导出    │
│  侧边   ├──────────────────────────────────────┤
│  导航   │  KPI 卡片区（单量/件数/金额/准时率…） │
│         ├──────────────────────────────────────┤
│ 看板    │  主图表区（趋势线 + 对比柱状）        │
│ 分析    │                                       │
│ 对比    │  下钻明细表（可排序/筛选/导出）       │
│ 设置    │                                       │
└─────────┴──────────────────────────────────────┘
```

- **看板页**：默认首页，展示当日与近 30 天趋势。
- **分析页**：维度切片下钻，点选渠道 / 品类 / 区域。
- **对比页**：选择对比类型与区间，输出对比结论。
- **设置页**：字段映射、目标值、数据备份与恢复。

---

## 8. 实施路线图

| 阶段 | 内容 | 预估周期 |
|------|------|----------|
| M1 骨架 | Electron + React 工程搭建，窗口与导航 | 3–5 天 |
| M2 数据 | SQLite 建表、Excel 导入与字段映射 | 5–7 天 |
| M3 看板 | KPI 卡片 + 趋势图 + 明细表 | 5–7 天 |
| M4 对比 | 环比/同比/目标达成/自定义对比 | 5–7 天 |
| M5 打磨 | 预警、导出、备份、安装包打包 | 5–7 天 |

> 合计约 4–6 周可交付首个可用版本（MVP）。

---

## 9. 待你确认的关键事项

以下问题会影响下一步落地，方案定稿前请帮我确认：

1. **数据来源**：目前发货明细主要从哪里来？（电商后台导出 Excel / ERP 系统 / 手动登记 / 平台 API）
2. **对比重点**：最常用的是"环比"还是"同比"？是否需要"目标达成率"？
3. **维度需求**：除了渠道、品类、区域，还需要按哪些维度分析（如物流商、仓库、业务员）？
4. **目标管理**：是否需要内置"日/月目标"设定功能？
5. **使用范围**：是个人用，还是需要多店铺 / 多账号隔离？

确认以上问题后，我可以直接进入搭建阶段，先产出可运行的 MVP 项目骨架。
